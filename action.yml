name: "Update status labels"
description: "Adds appropriate status label to issue"
branding:
  icon: filter
  color: red
runs:
  using: "composite"
  steps:
    - run: |
        bundle exec ruby run.rb
      shell: bash
